apiVersion: v1
kind: ConfigMap
metadata:
  name: chat-app-config
  namespace: {{ include "chat-app.name" . }}
  labels:
    {{- include "chat-app.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
